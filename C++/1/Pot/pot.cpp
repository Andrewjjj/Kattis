#include <iostream>
#include <math.h>
using namespace std;

int main(){
  int a;
  cin >> a;
  int b[a];
  for (int i=0, i<a; i++){
    cin >> b[i];
  }
  for (int i=0, i<a, i++){

  }
  a=pow(3,5);
  cout << a << endl;
}
